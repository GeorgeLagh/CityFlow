name: Temporary microfield empirical research

on:
  push:
    branches:
      - tmp/microfield-research-20260710
    paths:
      - 'tmp_microfield_runner/**'
      - '.github/workflows/tmp_microfield_research.yml'

concurrency:
  group: tmp-microfield-research-20260710
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tmp/microfield-research-20260710
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install numerical stack
        run: python -m pip install --upgrade pip numpy pandas
      - name: Run corrected cross-asset experiment
        shell: bash
        run: |
          mkdir -p tmp_microfield_results_bybit_corrected
          set +e
          python tmp_microfield_runner/run_bybit_corrected.py > tmp_microfield_results_bybit_corrected/RUN.log 2>&1
          code=$?
          echo "$code" > tmp_microfield_results_bybit_corrected/EXIT_CODE.txt
          set -e
      - name: Record environment
        if: always()
        run: |
          python --version > tmp_microfield_results_bybit_corrected/ENVIRONMENT.txt
          python -m pip freeze >> tmp_microfield_results_bybit_corrected/ENVIRONMENT.txt
      - name: Commit compact results
        if: always()
        run: |
          git config user.name 'microfield-research-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add tmp_microfield_results_bybit_corrected
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'chore: record corrected Bybit microfield results [skip ci]'
          git push origin HEAD:tmp/microfield-research-20260710
