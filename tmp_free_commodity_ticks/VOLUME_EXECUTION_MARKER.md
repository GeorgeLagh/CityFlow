# Volume execution marker

This branch is the corrected execution entry point. The downloader must emit `askVolume` and `bidVolume`; a one-day smoke gate blocks the full eight-instrument run otherwise.
